extends layout

block content
  h1= title
  p Welcome to #{title}
  if locals.token
    p token: #{token}
    form(method="post", action="/logout")
      input(type="submit", value="Logout")
  else
    a(href="/auth/github") sign in with github
